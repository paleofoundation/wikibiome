# Dependencies
node_modules/

# Raw source documents (PDFs, papers — too large for git)
raw/

# Archive files (raw.zip was 11GB and blocked deploys for 12 nights)
*.zip

# Deploy state markers (transient, regenerated each deploy)
.last-deploy
.queued-deploy-command.txt
.queued-deploy-stamp.txt
DEPLOY_QUEUED.txt
.autonomous-ingest.marker
.autonomous-ingest.lock.d/
.nightly-maintenance.lock.d/

# Build output directories
dist/
dist-preview/
preview/
wikibiome-build/
wikibiome-final/
wikibiome-preview/
wikibiome-v7b/
wikibiome-v7c/
wikibiome-v7d/
wikibiome-v7e/
wikibiome-v7f/
wikibiome-v7g/
wikibiome-v7h/

# Vite timestamp files
vite.config.js.timestamp-*

# Old JSX versions (keeping only v7)
wikibiome-poc.jsx
wikibiome-v5.jsx
wikibiome-v6.jsx
wikibiome-v7 2.jsx

# Auto-memory (session data)
.auto-memory/

# Intermediate lookup data
doi-lookup-results.json

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db

# Claude project config
.claude/
.fuse_hidden*
